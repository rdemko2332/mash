params {
  fastaSubsetSize = 2000
  bestRepFile = "$baseDir/data/best.fasta"
  inputDir = "$baseDir/data"
  outputDir = "$launchDir/output"
}

process {
    container = 'rdemko2332/mash'
}

docker {
    enabled = true
}